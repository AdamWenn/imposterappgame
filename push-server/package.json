{
  "name": "imposter-push-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "generate-vapid": "node -e \"import('web-push').then(({ default: webPush }) => console.log(JSON.stringify(webPush.generateVAPIDKeys(), null, 2)))\""
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "web-push": "^3.6.7"
  }
}
